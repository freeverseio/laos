apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "laos-evolution-node.fullname" . }}-test-connection"
  labels:
    {{- include "laos-evolution-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "laos-evolution-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
